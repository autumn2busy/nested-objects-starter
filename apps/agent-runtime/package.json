{
  "name": "@nested-objects/agent-runtime",
  "version": "0.1.0",
  "private": true,
  "description": "Production-safe Intelligence OS agent control-plane foundation for Nested Objects.",
  "type": "module",
  "engines": {
    "node": ">=22.16.0 <23"
  },
  "scripts": {
    "clean": "node ./scripts/clean.mjs",
    "build": "npm run clean && tsc -p tsconfig.json",
    "build:runtime": "nitro build",
    "dev": "nitro dev",
    "typecheck": "tsc -p tsconfig.json --noEmit && tsc -p tsconfig.api.json --noEmit",
    "test": "npm run build && tsc -p tsconfig.api.json --noEmit false --outDir dist-api && node --test test/*.test.mjs",
    "test:workflow": "npm run clean && vitest run --config vitest.workflow.config.ts",
    "format:check": "node ./scripts/format-check.mjs",
    "migration:check": "node ./scripts/validate-migration.mjs && node ./scripts/validate-phase-c-migration.mjs && node ./scripts/validate-durable-workflow-migration.mjs && node ./scripts/validate-operating-workflow-migration.mjs",
    "preview:check": "node ./scripts/validate-preview-runtime.mjs && node ./scripts/validate-preview-error-mapping.mjs",
    "specialists:check": "node ./scripts/validate-core-specialists.mjs",
    "validate": "npm run format:check && npm run dependency:check && npm run typecheck && npm run test && npm run test:workflow && npm run migration:check && npm run preview:check && npm run specialists:check",
    "dependency:check": "node ./scripts/dependency-smoke.mjs"
  },
  "dependencies": {
    "@openai/agents": "0.17.0",
    "@supabase/supabase-js": "2.112.3",
    "@workflow/core": "4.8.5",
    "nitro": "3.0.260610-beta",
    "workflow": "4.8.5",
    "zod": "4.4.3"
  },
  "devDependencies": {
    "@workflow/vitest": "4.0.21",
    "typescript": "5.9.3",
    "vitest": "4.1.11"
  }
}
