diff --git a/apps/web-members/app/welcome-back/WelcomeBackView.tsx b/apps/web-members/app/welcome-back/WelcomeBackView.tsx
new file mode 100644
index 0000000..992a0e7
--- /dev/null
+++ b/apps/web-members/app/welcome-back/WelcomeBackView.tsx
@@ -0,0 +1,277 @@
+'use client'
+
+import { useAuth } from '@/components/auth-provider'
+import { PLAN_UIDS } from '@/lib/plan-config'
+import Link from 'next/link'
+import {
+  CheckCircle,
+  ArrowRight,
+  Shield,
+  BookOpen,
+  Search,
+  Bot,
+  FileText,
+  Briefcase,
+  Star,
+  HelpCircle,
+} from 'lucide-react'
+
+const UPGRADE_FEATURES = [
+  {
+    icon: Search,
+    title: '200+ Verified Hiring Firms',
+    description: 'Searchable, filterable directory with real pay data, contact info, and coverage areas.',
+  },
+  {
+    icon: BookOpen,
+    title: 'Structured Training Tracks',
+    description: 'Role-based modules from beginner to advanced — complete quizzes, earn your Trust Score.',
+  },
+  {
+    icon: Bot,
+    title: 'AI Concierge',
+    description: 'Ask anything about firms, routes, or requirements and get grounded answers instantly.',
+  },
+  {
+    icon: FileText,
+    title: 'AI Resume Builder',
+    description: 'ATS-optimized resumes built for inspectors, notaries, and field service pros.',
+  },
+  {
+    icon: Briefcase,
+    title: 'Live Job Board',
+    description: 'Updated job listings with advanced filters by location, pay, and specialty.',
+  },
+  {
+    icon: Shield,
+    title: 'Trust Score & Badge',
+    description: 'Complete training, verify your background, and earn a visible Trust Score that firms recognize.',
+  },
+]
+
+const MIGRATION_STEPS = [
+  {
+    step: 1,
+    title: 'Click "Claim Your Account" below',
+    detail: 'You\'ll create your login on the new platform. Use the same email you used on the old site.',
+  },
+  {
+    step: 2,
+    title: 'Set your password',
+    detail: 'Choose a new password. Your old Wix login won\'t carry over — this is a fresh, faster system.',
+  },
+  {
+    step: 3,
+    title: 'Enter your card for the Founders plan ($37/year)',
+    detail:
+      'Your early adopter price is locked in for life. The new system uses Stripe for secure billing.',
+  },
+  {
+    step: 4,
+    title: 'Explore your new dashboard',
+    detail: 'Everything is waiting for you — directory, training, AI tools, and your profile.',
+  },
+]
+
+function FoundersBadge() {
+  return (
+    <span className="inline-flex items-center gap-1.5 rounded-full border border-amber-300 bg-amber-50 px-3 py-1 text-xs font-semibold text-amber-800 shadow-sm">
+      <Star className="h-3 w-3 fill-amber-400 text-amber-400" />
+      Founding Member
+    </span>
+  )
+}
+
+export function WelcomeBackView() {
+  const { isAuthenticated, user } = useAuth()
+
+  const openFoundersSignup = () => {
+    if (typeof window !== 'undefined' && window.Outseta?.auth?.open) {
+      window.Outseta.auth.open({
+        widgetMode: 'register',
+        planUid: PLAN_UIDS.FOUNDERS,
+        planPaymentTerm: 'year',
+        skipPlanOptions: true,
+      })
+    } else {
+      window.location.href = `https://nested-objects.outseta.com/auth?widgetMode=register&planUid=${PLAN_UIDS.FOUNDERS}#o-anonymous`
+    }
+  }
+
+  return (
+    <main className="mx-auto max-w-4xl px-4 py-10 sm:px-6 lg:px-8 lg:py-16">
+      {/* Hero */}
+      <header className="text-center">
+        <FoundersBadge />
+        <h1 className="mt-6 text-3xl font-bold tracking-tight text-slate-900 sm:text-4xl md:text-5xl">
+          Your membership just got a{' '}
+          <span className="text-brand-copper">massive upgrade.</span>
+        </h1>
+        <p className="mx-auto mt-4 max-w-2xl text-base text-slate-600 sm:text-lg">
+          We rebuilt Nested Objects from the ground up — new dashboard, AI tools, structured
+          training, and a faster directory. Your Founding Member price stays locked at{' '}
+          <strong>$37/year for life.</strong>
+        </p>
+      </header>
+
+      {/* Already logged in */}
+      {isAuthenticated && (
+        <div className="mx-auto mt-8 max-w-xl rounded-2xl border border-emerald-200 bg-emerald-50 p-6 text-center">
+          <CheckCircle className="mx-auto h-8 w-8 text-emerald-600" />
+          <h2 className="mt-3 text-lg font-semibold text-emerald-900">
+            You are already signed in!
+          </h2>
+          <p className="mt-1 text-sm text-emerald-700">
+            Your account is set up and ready to go.
+          </p>
+          <Link
+            href="/inspector-dashboard"
+            className="mt-4 inline-flex items-center gap-2 rounded-lg bg-emerald-600 px-5 py-2.5 text-sm font-semibold text-white transition hover:bg-emerald-500"
+          >
+            Go to your dashboard <ArrowRight className="h-4 w-4" />
+          </Link>
+        </div>
+      )}
+
+      {/* What's new grid */}
+      <section className="mt-12">
+        <h2 className="text-center text-xl font-semibold text-slate-900 sm:text-2xl">
+          Everything that&apos;s new in your membership
+        </h2>
+        <div className="mt-8 grid gap-5 sm:grid-cols-2 lg:grid-cols-3">
+          {UPGRADE_FEATURES.map((feature) => {
+            const Icon = feature.icon
+            return (
+              <div
+                key={feature.title}
+                className="rounded-xl border border-slate-200 bg-white p-5 shadow-sm"
+              >
+                <div className="flex h-10 w-10 items-center justify-center rounded-lg bg-brand-copper/10">
+                  <Icon className="h-5 w-5 text-brand-copper" />
+                </div>
+                <h3 className="mt-3 text-sm font-semibold text-slate-900">{feature.title}</h3>
+                <p className="mt-1 text-sm text-slate-600">{feature.description}</p>
+              </div>
+            )
+          })}
+        </div>
+      </section>
+
+      {/* Migration steps */}
+      {!isAuthenticated && (
+        <section className="mt-14">
+          <h2 className="text-center text-xl font-semibold text-slate-900 sm:text-2xl">
+            How to claim your new account
+          </h2>
+          <p className="mx-auto mt-2 max-w-xl text-center text-sm text-slate-500">
+            Takes about 2 minutes. Use the same email you used on the old site so we can
+            recognize you as a Founding Member.
+          </p>
+
+          <div className="mx-auto mt-8 max-w-2xl space-y-4">
+            {MIGRATION_STEPS.map((step) => (
+              <div
+                key={step.step}
+                className="flex gap-4 rounded-xl border border-slate-200 bg-white p-5"
+              >
+                <div className="flex h-8 w-8 shrink-0 items-center justify-center rounded-full bg-brand-copper text-sm font-bold text-white">
+                  {step.step}
+                </div>
+                <div>
+                  <h3 className="text-sm font-semibold text-slate-900">{step.title}</h3>
+                  <p className="mt-1 text-sm text-slate-600">{step.detail}</p>
+                </div>
+              </div>
+            ))}
+          </div>
+        </section>
+      )}
+
+      {/* CTA */}
+      {!isAuthenticated && (
+        <section className="mt-12 rounded-2xl bg-slate-900 px-6 py-10 text-center text-slate-50">
+          <FoundersBadge />
+          <h2 className="mt-4 text-2xl font-semibold sm:text-3xl">
+            Ready to claim your upgraded account?
+          </h2>
+          <p className="mx-auto mt-3 max-w-xl text-sm text-slate-300">
+            Same email. Same $37/year price. A completely rebuilt platform with tools that
+            didn&apos;t exist before.
+          </p>
+          <button
+            type="button"
+            onClick={openFoundersSignup}
+            className="mt-6 inline-flex items-center gap-2 rounded-lg bg-brand-copper px-8 py-3.5 text-base font-semibold text-white shadow-sm transition hover:bg-brand-copperDark focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-brand-copper focus-visible:ring-offset-2 focus-visible:ring-offset-slate-900"
+          >
+            Claim Your Founders Account <ArrowRight className="h-5 w-5" />
+          </button>
+          <p className="mt-3 text-xs text-slate-400">
+            Secure checkout powered by Stripe. Cancel or change plans anytime.
+          </p>
+        </section>
+      )}
+
+      {/* FAQ */}
+      <section className="mt-14">
+        <h2 className="text-center text-xl font-semibold text-slate-900">
+          Common questions about the migration
+        </h2>
+        <div className="mx-auto mt-6 max-w-2xl space-y-5 text-sm text-slate-700">
+          <div>
+            <h3 className="flex items-center gap-2 font-semibold text-slate-900">
+              <HelpCircle className="h-4 w-4 text-brand-copper" />
+              Why do I need to create a new account?
+            </h3>
+            <p className="mt-1.5 pl-6">
+              We moved from Wix to a custom-built platform for speed, AI tools, and a better
+              experience. Your old Wix login can&apos;t transfer, but creating your new account
+              takes about 2 minutes and your Founding Member pricing stays the same.
+            </p>
+          </div>
+          <div>
+            <h3 className="flex items-center gap-2 font-semibold text-slate-900">
+              <HelpCircle className="h-4 w-4 text-brand-copper" />
+              Will I lose my data or access?
+            </h3>
+            <p className="mt-1.5 pl-6">
+              No. Your directory access, firm data, and membership status carry over. You
+              just need to set up a login on the new system.
+            </p>
+          </div>
+          <div>
+            <h3 className="flex items-center gap-2 font-semibold text-slate-900">
+              <HelpCircle className="h-4 w-4 text-brand-copper" />
+              Do I have to pay again?
+            </h3>
+            <p className="mt-1.5 pl-6">
+              You&apos;ll enter your card on the new system (Stripe handles billing now, not
+              Wix). Your rate stays at $37/year — the Founding Member price is locked for
+              life. We recommend canceling your old Wix subscription after you&apos;re set up
+              on the new site.
+            </p>
+          </div>
+          <div>
+            <h3 className="flex items-center gap-2 font-semibold text-slate-900">
+              <HelpCircle className="h-4 w-4 text-brand-copper" />
+              What if I need help?
+            </h3>
+            <p className="mt-1.5 pl-6">
+              Reply to any email from us or use the Contact page. Founding Members get
+              priority support — we&apos;ll get you sorted fast.
+            </p>
+          </div>
+        </div>
+      </section>
+
+      {/* Footer link */}
+      <div className="mt-10 border-t border-slate-200 pt-6 text-center">
+        <Link
+          href="/"
+          className="text-sm font-medium text-brand-copper underline underline-offset-4 hover:text-brand-copperDark"
+        >
+          ← Back to home
+        </Link>
+      </div>
+    </main>
+  )
+}
diff --git a/apps/web-members/app/welcome-back/page.tsx b/apps/web-members/app/welcome-back/page.tsx
new file mode 100644
index 0000000..c5cedda
--- /dev/null
+++ b/apps/web-members/app/welcome-back/page.tsx
@@ -0,0 +1,14 @@
+import { generatePageMetadata } from '@/lib/seo'
+import type { Metadata } from 'next'
+import { WelcomeBackView } from './WelcomeBackView'
+
+export const metadata: Metadata = generatePageMetadata({
+  title: 'Welcome Back — Claim Your New Account | Nested Objects',
+  description:
+    'Your Nested Objects membership just got a massive upgrade. Claim your Founding Member account on the new platform — same price, way more tools.',
+  path: '/welcome-back',
+})
+
+export default function WelcomeBackPage() {
+  return <WelcomeBackView />
+}
